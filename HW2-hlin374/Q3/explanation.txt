Explanation:

Using nonlinear scales can help us distinguish some linearly close data. The distance between them on the graph can be larger. So we can see them separately. And they will smoothen the variation. The distance between large and small values will reduce as well.